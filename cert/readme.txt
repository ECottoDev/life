this is where the certifications should be.

if you receive .pem certifications you can change them to .cert and .key files so they can be used without a problem with the app

you would need to change permissions as well so they are able to be read by node.js

use chmod 666 for both.